@include path/to/the/file

<source>
  @type syslog
  port 27016
  tag rsyslog

  <parse>
    @type syslog
  </parse>
</source>

<match myapp.access>
  @type file
  path /var/log/fluent/myapp/access
</match>
